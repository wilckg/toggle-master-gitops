apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: toggle-master
spec:
  selector:
    app: flag-service
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
  type: ClusterIP